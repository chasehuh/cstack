---
description: Opus subagents — pointer to shared orchestration SoT (Claude Code CLI)
alwaysApply: true
---

# Opus subagents → shared SoT

Do not fork Opus routing or monitoring here. Source of truth:

`~/.agents/skills/sume-main-agent-orchestration/SKILL.md`

§ **Opus transport — Claude Code CLI** and § **Cursor-only — Opus /
background-worker monitoring**.

From a Cursor main agent: Opus workers use `claude-human-stream` (background
Shell). Grok land / Grok author use `agent-human-stream --backend grok`
(same Shell recipe — not Cursor `Task`). Monitor via completion
notification — no polling watchers. Other harnesses ignore Cursor-only
sections.
